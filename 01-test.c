#include <stdio.h>

int main(){
    // char name[10];
    // scanf("%s",name);
    // printf("Hello %s\n",name); 

    // char firstName[10];
    // char lastName[10];
    // scanf("%s%s",firstName,lastName);
    // printf("Hello %s %s\n",firstName,lastName); 

    // double number;
    // number = 999.999;
    // int nr2;
    // nr2 = (float)(int) (number);
    // printf("The number is %i\n",nr2);

    // char name[20];
    // fgets(name,10,stdin);
    // printf("name: %s\n",name);
}